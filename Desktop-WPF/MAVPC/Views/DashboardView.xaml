<UserControl x:Class="MAVPC.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="PANEL DE CONTROL GENERAL" 
                       Foreground="{DynamicResource PrimaryHueMidBrush}" 
                       FontSize="14" FontWeight="Bold"/>
            <TextBlock Text="Resumen de Operaciones en Tiempo Real" 
                       Foreground="White" Opacity="0.6" FontSize="12"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1E2129" CornerRadius="10" Margin="0,0,10,0" Padding="20">
                <StackPanel>
                    <materialDesign:PackIcon Kind="CarMultiple" Foreground="#FF3D71" Width="30" Height="30"/>
                    <TextBlock Text="{Binding TraficoNivel}" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="Densidad Tráfico" Foreground="Gray" FontSize="11"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#1E2129" CornerRadius="10" Margin="5,0,5,0" Padding="20">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Cctv" Foreground="#00E5FF" Width="30" Height="30"/>
                    <TextBlock Text="{Binding CamarasActivas}" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="Cámaras Online" Foreground="Gray" FontSize="11"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#1E2129" CornerRadius="10" Margin="5,0,5,0" Padding="20">
                <StackPanel>
                    <materialDesign:PackIcon Kind="AlertCircle" Foreground="#FFC107" Width="30" Height="30"/>
                    <TextBlock Text="{Binding IncidentesHoy}" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="Incidentes (24h)" Foreground="Gray" FontSize="11"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Background="#1E2129" CornerRadius="10" Margin="10,0,0,0" Padding="20">
                <StackPanel>
                    <materialDesign:PackIcon Kind="ServerNetwork" Foreground="#00E676" Width="30" Height="30"/>
                    <TextBlock Text="ONLINE" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,10,0,0"/>
                    <TextBlock Text="Estado Servidor" Foreground="Gray" FontSize="11"/>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#15171E" CornerRadius="15" BorderBrush="#333" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border BorderBrush="#333" BorderThickness="0,0,0,1" Padding="20">
                    <DockPanel>
                        <TextBlock Text="MAPA DE CALOR - ZONA CENTRO" Foreground="White" FontWeight="SemiBold"/>
                        <materialDesign:PackIcon Kind="MapMarker" HorizontalAlignment="Right" Foreground="Gray"/>
                    </DockPanel>
                </Border>

                <Grid Grid.Row="1">
                    <TextBlock Text="[ VISUALIZACIÓN DE MAPA EN CONSTRUCCIÓN ]" 
                               Foreground="#333" FontSize="20" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>

                    <Border BorderBrush="{DynamicResource PrimaryHueMidBrush}" BorderThickness="1" CornerRadius="50" Width="200" Height="200" Opacity="0.1">
                        <Border BorderBrush="{DynamicResource PrimaryHueMidBrush}" BorderThickness="1" CornerRadius="50" Width="150" Height="150"/>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>