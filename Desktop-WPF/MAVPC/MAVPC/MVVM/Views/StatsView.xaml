<UserControl x:Class="MAVPC.MVVM.Views.StatsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1000">

    <UserControl.Resources>
        <Style x:Key="KpiCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#333"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChartCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2A2A2A"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="4" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="DASHBOARD ANALÍTICO" Foreground="White" FontSize="26" FontWeight="Bold"/>
            <TextBlock Text="Resumen de actividad en tiempo real" Foreground="Gray" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource KpiCardStyle}" BorderBrush="#00FFFF">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <materialDesign:PackIcon Kind="ChartTimeline" Foreground="#00FFFF" Width="20" Height="20" VerticalAlignment="Center"/>
                        <TextBlock Text="TOTAL INCIDENCIAS" Foreground="Gray" FontSize="11" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding KpiTotal}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource KpiCardStyle}" BorderBrush="#FF007F">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <materialDesign:PackIcon Kind="AlertCircle" Foreground="#FF007F" Width="20" Height="20" VerticalAlignment="Center"/>
                        <TextBlock Text="NIVEL CRÍTICO" Foreground="Gray" FontSize="11" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding KpiCriticas}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource KpiCardStyle}" BorderBrush="#FFA500">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <materialDesign:PackIcon Kind="Car" Foreground="#FFA500" Width="20" Height="20" VerticalAlignment="Center"/>
                        <TextBlock Text="CAUSA PRINCIPAL" Foreground="Gray" FontSize="11" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Viewbox Height="38" HorizontalAlignment="Left">
                        <TextBlock Text="{Binding KpiTopCausa}" Foreground="White" FontWeight="SemiBold"/>
                    </Viewbox>
                </StackPanel>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource KpiCardStyle}" Margin="0" BorderBrush="#32CD32">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <materialDesign:PackIcon Kind="MapMarkerRadius" Foreground="#32CD32" Width="20" Height="20" VerticalAlignment="Center"/>
                        <TextBlock Text="ZONA CALIENTE" Foreground="Gray" FontSize="11" FontWeight="Bold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <TextBlock Text="{Binding KpiTopZona}" Foreground="White" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Style="{StaticResource ChartCardStyle}" Margin="0,0,20,0">
                <Grid>
                    <TextBlock Text="Top 7 Vías Conflictivas" Foreground="#00FFFF" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Top"/>

                    <lvc:CartesianChart Series="{Binding CarreterasSeries}" LegendLocation="None" Margin="0,40,0,0">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Foreground="Gray" ShowLabels="False">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Step="1" StrokeThickness="0"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Labels="{Binding CarreterasLabels}" Foreground="White" FontSize="13">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Step="1" Stroke="#22FFFFFF" StrokeThickness="1"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.DataTooltip>
                            <lvc:DefaultTooltip SelectionMode="SharedYValues" Background="#222" Foreground="White" BulletSize="10"/>
                        </lvc:CartesianChart.DataTooltip>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource ChartCardStyle}" Margin="0,0,0,10">
                <Grid>
                    <TextBlock Text="Distribución por Tipo" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Top"/>

                    <lvc:PieChart Series="{Binding IncidenciasSeries}" 
                                  LegendLocation="Right" 
                                  InnerRadius="60" 
                                  HoverPushOut="10"
                                  Margin="0,20,0,0">
                        <lvc:PieChart.ChartLegend>
                            <lvc:DefaultLegend Foreground="#AAA" BulletSize="10"/>
                        </lvc:PieChart.ChartLegend>
                        <lvc:PieChart.DataTooltip>
                            <lvc:DefaultTooltip Background="#222" Foreground="White"/>
                        </lvc:PieChart.DataTooltip>
                    </lvc:PieChart>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource ChartCardStyle}" Margin="0,10,0,0">
                <Grid>
                    <TextBlock Text="Volumen de Activos" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Top"/>

                    <lvc:CartesianChart Series="{Binding CamarasSeries}" LegendLocation="None" Margin="0,30,0,0">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding Labels}" Foreground="White" FontSize="12">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Step="1" StrokeThickness="0"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Foreground="Gray" LabelFormatter="{Binding Formatter}">
                                <lvc:Axis.Separator>
                                    <lvc:Separator Stroke="#22FFFFFF"/>
                                </lvc:Axis.Separator>
                            </lvc:Axis>
                        </lvc:CartesianChart.AxisY>
                        <lvc:CartesianChart.DataTooltip>
                            <lvc:DefaultTooltip SelectionMode="SharedYValues" Background="#222" Foreground="White"/>
                        </lvc:CartesianChart.DataTooltip>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>